# Session Log — 2026-09-11 → 09-12

Everything done, in order. Servers at the end. No secrets in this file.

---

## 1. ChatGPT hero package — studied & prototyped

- Unpacked `~/Documents/Codex/2026-09-11/i/outputs/olivia-arcana-hero/` (Olivia-Arcana-Hero-Package.zip): temple-scene hero, WebGL water shader, self-hosted fonts (EN+UA), full i18n/a11y, design-system doc.
- Recovered your attached Olivia scene (1672×941) — wasn't saved anywhere on disk; extracted from session transcript; later found original at `~/Downloads/34EE5F88-….PNG`.
- **Built `~/olivia-arcana/hero-lab/`** (standalone HTML, server :8791): "The Approach" — 400svh pin, three zones (hold → approach → arrival), true dolly curve `1/(1−0.545s)`, time-corrected damping (same feel at 60/120Hz), crescent-halo rises with her, caption I→II crossfade, gilt arrival glow, EN/UA, mobile, reduced-motion.
- Figure quality pass: Runware 4× upscale + original alpha as matte (edge pixels inherit true scene colors — zero fringe).
- **Judge panel workflow (23 agents)** → 19 confirmed findings, all fixed (easing direction, dead zone, rAF-independent damping, DM Sans has **no Cyrillic** → added IBM Plex Sans Cyrillic fallback, ✦ not ✧, copy register…).
- **Water motion**: ported temple water shader to the Olivia scene — horizon frozen, sea shimmers; her wake rings live IN the shader (displace real water + reflections, track her feet, widen as she nears); hem dissolves through a waterline mask; levitation bob (8.5s) + reflection wobble (7s). "She drifts through water, not a cutout scaling."

## 2. Codex's own build — picked up & finished (server :8792)

- Its architecture: one-shader compositor — frame 0 = untouched photo; crossfade to [inpainted clean plate + green-keyed figure]; perspective growth to 2.94×; live re-rendered reflection through moving water; hem sway; hand-drip.
- Its bug (admitted before quota died): regenerated "impostor" Olivia → identity pop. **Fixed**: real-Olivia green-screen sprite from my matte; geometry constants nearly matched theirs (530/758.5 vs their 531/756); foot fraction .54→.50.
- Second real bug found: init gated on `img.decode()` with silent catch — hangs forever on big data-URIs/hidden tabs → animation silently never starts. Hardened (load-settled + 800ms decode race).
- **Sprite quality ("340p vs 1080p")**: Seedream 5 Pro reference-remaster ("change nothing, only sharpen") → 2048×1984 true-detail sprite; re-matted; contact-centroid .511 ≈ code's .50. Crossfade reads "she stirs to life", arrival matches background fidelity. (Side effect: fuller moon-disc halo — say if you want the thin crescent re-rolled.)
- Backups kept: `work/generated-v2/olivia-key-CODEX-REGEN.png.bak`, `work/v2/scene.js.bak`. Debug hook `#p=0.6` added.

## 3. Production site rebuilt to the Arrival design (server :3300)

### Palette & system
- Site-wide sweep (135+ files total): old liquid-glass night (#04060e/#080d1c/#16295c, azure #4b84ff, cyan #6fe0ff, old gold #d4af37) → **ultramarine system**: abyss `#0a0d38`, night `#10134d`, deep `#181d7a`, lapis `#20279b`, moonstone `#e8e9ff`, mist `#b7bce9`, cool `#8d97ff`, ONE gilt `#e0b768`. All rgba triplet variants mapped too.
- **Liquid glass retired in place** (class names kept → ~80 components flipped for free): `.glass`/`.glass-thin` = flat hairline-ruled plates, no blur; `.glass-btn`= hairline secondary; `.is-gilt`/`.btn-ink` = flat gilt rectangle (hover lift). Aurora killed twice (CSS lobes + WebGPU shader → still abyss→night wash).
- Opaque `html,body` ground painted (was UA #121212 showing through transitions/overscroll — audit-verified root cause). Text tokens unified to moonstone.

### Hero
- `src/components/hero/TheArrival.tsx` — Codex compositor ported to React/TS (full WebGL pass, wake, reflection, drip), assets in `public/arrival/`, site copy/i18n wired, `#p=` QA hook, RM = static scene + no GL, sea dissolves into almanac night (seam gradient), old opening (SkyChart + ScrollCinema mount) replaced.

### Chrome
- `Navbar.tsx` → Arrival masthead (serif tracked wordmark, hairline-underline nav, gilt rect CTA; pills/blur/magnetics gone).
- `Footer.tsx` → colophon band (✦ mono note / serif italic / wordmark) + quiet mono directory + legal row.

### Audit + conversion workflows (24 + 7 agents)
- Audit: 33 findings, adversarially verified. Conversion agents fixed the deep pockets: daily flip card, oracle card backs/faces (two-golds bug), home spread theater, academy (#c8a84b gold, #4ecdc4 teal, Material rainbow → restrained set), form controls, page-turn sheet, LegalShell rust CTA→gilt, share-card canvas, cosmos pre-hydration blank. Plus sweeps: banned fallbacks (28 files), blue-black shadows (7), stray near-blacks.
- Homepage: dead SkyChart flash buttons removed; panorama −30svh under-pin overlap retired. (~1500 lines dead SkyChart code still in page.tsx — bundle-only, excise on your word.)

### Infra gotcha that ate an hour (bank this)
- Turbopack's Tailwind v4 scanner reads **binary build artifacts** as class candidates → `Unexpected token \u{18}` CSS parse error, every route 500. Poison sources: `.netlify/`, `out/`, un-gitignored `.next-trash`. Fix: move them out of the repo (now parked in session scratchpad; restore = move back or rebuild with `npm run build`).

## 4. Shaders.com layer (v1 — you rated 2/100, kept but cranked)

- `src/components/arrival/`: **MoonlitSea** (living water divider after Fig. 0), **StarMotes** (cursor-gust motes on Inscription), **OracleMist** (/oracle mist parts around cursor), **CrescentSeal** (breathing gilt/peri irradiance moon above both footers), + **godrays** at hero arrival (moonstone rays from her crescent, wake past 50% approach — verified rendering).
- All: RM static, offscreen unmounted, no-WebGPU fallback. Dependency: agent bumped `shaders` 3.1.463→3.2.470 (Irradiance missing in old dist; within ^range, package.json untouched, lockfile updated).

## 5. CARTA COELI — the signature (current flagship)

Research (Awwwards 2026: Lando Norris SOTY, Hubtown cursor-reveal, Lusion fluid) → winners own ONE structural interaction. Built:

- **`src/components/sky/voyage.ts`** — contract: 15 pages berthed at REAL constellations (Oracle→Cassiopeia, Daily→Ursa Minor, Tariff→Libra, Academy→Lyra, Story→Pegasus, Ask→Aquila, …), events `oa-sky-fly/arrive/map`.
- **`SkyVoyageCanvas.tsx`** — persistent real-sky canvas under the whole site (site's own 109-star catalog + sidereal math): stereographic camera, all constellations hairline, current page's figure gilt + cartouche, cursor-lantern (stars brighten near pointer), twinkle, sidereal drift.
- **Flight navigation** — every internal nav (`TransitionLink`) flies the camera along the great circle to the destination constellation (950ms house ease, fov breath, gilt lines draw on arrival) under the page turn.
- **`SkyAtlas.tsx` — press M** (or "THE SKY ✦ M", bottom-right): the whole site as an engraved star chart — graticule, every figure, cartouches naming each page, tonight's actual meridian ruled in gilt, hover lifts, click flies + navigates. Focus-trapped dialog, EN/UA, RM instant.
- Content grounds now 88% translucent → stars whisper through every page.

## Servers (all local, nothing deployed)

| URL | What |
|---|---|
| localhost:**3300** | **The site** — full rebuild + Carta Coeli (`npm run dev` in `~/olivia-arcana/website`, port 3300) |
| localhost:8791 | hero-lab standalone prototype |
| localhost:8792 | Codex build, finished |

Session servers die with me; restart: `cd ~/olivia-arcana/website && npm run dev -- --port 3300`.

## Open / pending
- Deploy: NOTHING deployed — everything since 6a93199a is local-only, your word required.
- Dead SkyChart excision in page.tsx; thin-crescent halo re-roll; shaders-lib version decision; Safari/iOS + real-mobile pass on TheArrival (per Codex acceptance list); oracle visual pass in a real browser (hidden-pane/headless can't).
- Next-wave shader menu (5–10) untouched: engraving-plate card hover, water-shelf reflections, daily-card cursor ripples, frost-locked tiers, ink-flow /ask.
- Vault note: `brain/Olivia Arcana.md` is missing from SerhiiVault (with North Star etc.) — not recreated without your word; this file is the session record meanwhile.
- Rotate keys when convenient: Runware key (in an old transcript), shaders + Stitch keys in `~/.mcp.json`.
